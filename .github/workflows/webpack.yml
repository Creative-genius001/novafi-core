name: Novafi CI

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']
env:
  NODE_ENV: test
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  REDIS_HOST: localhost
  REDIS_PORT: 6379
  JWT_ACCESS_SECRET: ${{ secrets.JWT_ACCESS_TOKEN_SECRET }}
  JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_TOKEN_SECRET }}
  SUMSUB_API_TOKEN: ${{ secrets.SUMSUB_API_TOKEN }}
  SUMSUB_API_SECRET: ${{ secrets.SUMSUB_API_SECRET }}
  SUMSUB_BASE_URL: https://api.sumsub.com

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Linting
        run: npm run lint
        env:
          CI: true

      - name: Cache Build Output
        uses: actions/cache@v4
        with:
          path: dist
          key: ${{ runner.os }}-build-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-build-

      - name: Build
        run: |
          npm install
          npx webpack
